import { test } from "@playwright/test";

test.describe("full crawl health", () => {
  test("bootstrap placeholder", async () => {
    test.skip(true, "Full-run implementation starts in the next phase.");
  });
});
